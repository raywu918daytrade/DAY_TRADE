"""
交易相關設定 — cnn 策略專用常數，跟 rally/orb/mkt 各自獨立，不共用。

只放常數，不放邏輯，避免其他模組互相依賴造成 circular import。
"""

import torch

# ── Triple Barrier 參數（標籤怎麼定義） ──────────────────────────────────────
# 沿用 rally/orb 的起始值，之後用 evaluate() 的結果再調整。
TP_PCT = 0.03
SL_PCT = 0.03
HOLD_BARS = 30

# ── 五路多解析度分支的 lookback 長度 ───────────────────────────────────────
# 五路對齊到同一段「過去30分鐘」的時間範圍，只是取樣密度不同：
#   m1/m3/m5（rolling）是每分鐘一列，抓過去30點＝過去30分鐘
#   m3_std 是獨立3分K棒，抓過去10根＝30分鐘；m5_std 獨立5分K棒抓過去6根＝30分鐘
#
# 2026-07-24 從60分鐘改成30分鐘：(1) 讓CNN內部感受野的目標從60分鐘降到30分鐘，
# 需要疊的層數/dilation倍率更少，capacity增加得比較保守，overfitting風險較低；
# (2) 開盤9:00+30分鐘＝9:30就能推論，比60分鐘版本的9:00+60分鐘=10:00提早涵蓋到
# 更多9-10點活躍時段的訊號（見討論）。
LOOKBACK_MINUTES = 30
M1_LEN = 30
M3_LEN = 30
M5_LEN = 30
M3STD_LEN = 10
M5STD_LEN = 6

# ── 流動性篩選 ────────────────────────────────────────────────────────────
# 沿用 orb/mkt 的起始門檻（20日均量 ≥ 1000張）。
MIN_VOL_MA20 = 1_000_000

# ── 訓練裝置 ──────────────────────────────────────────────────────────────
DEVICE = "mps" if torch.backends.mps.is_available() else "cpu"
