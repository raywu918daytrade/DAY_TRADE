"""
交易相關設定 — cnn 策略專用常數，跟 rally/orb/mkt 各自獨立，不共用。

只放常數，不放邏輯，避免其他模組互相依賴造成 circular import。
"""

import torch

# ── Triple Barrier 參數（標籤怎麼定義） ──────────────────────────────────────
# 沿用 rally/orb 的起始值，之後用 evaluate() 的結果再調整。
TP_PCT = 0.03
SL_PCT = 0.03
HOLD_BARS = 30

# ── 五路多解析度分支的 lookback 長度 ───────────────────────────────────────
# 五路對齊到同一段「過去60分鐘」的時間範圍，只是取樣密度不同：
#   m1/m3/m5（rolling）是每分鐘一列，抓過去60點＝過去60分鐘
#   m3_std 是獨立3分K棒，抓過去20根＝60分鐘；m5_std 獨立5分K棒抓過去12根＝60分鐘
LOOKBACK_MINUTES = 60
M1_LEN = 60
M3_LEN = 60
M5_LEN = 60
M3STD_LEN = 20
M5STD_LEN = 12

# ── 流動性篩選 ────────────────────────────────────────────────────────────
# 沿用 orb/mkt 的起始門檻（20日均量 ≥ 1000張）。
MIN_VOL_MA20 = 1_000_000

# ── 訓練裝置 ──────────────────────────────────────────────────────────────
DEVICE = "mps" if torch.backends.mps.is_available() else "cpu"
